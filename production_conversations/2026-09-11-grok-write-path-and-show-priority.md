# Production Conversation Log — 2026-09-11 (Grok write path + show priority)

**Logged by:** Grok (xAI)  
**Repo:** mhvnsnt/TRIPPEDD-Production-studios-

## Write path

Grok GitHub OAuth connector remains read-only (`403 Resource not accessible by integration`).  
Bypass: fine-grained PAT → `tools`-local `grok_github_push.py` / Contents API.

Confirmed: `mhvnsnt` auth, `push: true` on this repo. Oral protrusion fix landed on `main`.

ChatGPT / Claude keep using their connectors. Grok can push directly when given a scoped PAT for the session.

## Show priority (do this order)

1. **Oral geometry truth** — bridge + survey PASS (no pink plank past lip plane)
2. **Jaw / aperture measurable** — REST vs JAW_OPEN separation, teeth/tongue hits behind plane
3. **Rhubarb fail-closed** — real JSON cues or no claimed lip-sync
4. **Evidence packet** — frames on disk + contact sheet + hashes (no telemetry as creative_final)
5. **Memory-safe first shot** — seed `742918`, 640×360, 8 frames, Gaussian **off**, `MARS_CANONICAL` hash recorded
6. **Then** Gaussian / OpenUSD `ParticleField3DGaussianSplat` region on the same seed
7. **Then** widen environment + performance + Adult Swim episode packaging

Blockage law still holds: diagnose → alternative → execute recovery → preserve failure evidence → only then report.

## Creative register

God Molecule is allowed to be **Adult Swim–grade**: cosmic dread, body-horror comedy, sincerity and stupidity in the same shot, Mars as unreliable narrator of infinite nested worlds. Production systems stay strict (hashes, seeds, fail-closed); the *tone* of the show does not.

Identity rule unchanged: **MARS_CANONICAL is immutable**. Comedy is performance, lighting, edit, dialogue, world logic — not a swapped face.

## OSS already on the map

- World seed / voxel: luanti, worldgen  
- Capture: COLMAP, Meshroom, Nerfstudio, gsplat  
- Scene: Blender, OpenUSD 26.03+ ParticleField3DGaussianSplat  
- Character oral: GNM donors + TRIPPEDD bridge/survey  
- Lip-sync: Rhubarb  

Next code slices: oral runner wired to survey, PLY→particle-field wrapper, creative direction note for writers/animators.
